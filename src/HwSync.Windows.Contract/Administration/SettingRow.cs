namespace HwSync.Windows.Contract.Administration
{
    /// <summary>
    /// Подпись и значение настройки сервера для просмотра.
    /// </summary>
    public sealed record SettingRow(string Name, string Value);
}
